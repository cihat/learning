#include <iostream>
// #include <cmath>

int main() {
  using namespace std;

  double x = 3;
  double y = 4;
  double z;

  // z = max(x, y);
  // z = min(x, y);
  // z = pow(2, 4);
  z = sqrt(9);
  z = abs(-23);
  z = round(3.4);
  z = ceil(4.5);
  z = floor(4.9);

  cout << z;

  return 0;
}
